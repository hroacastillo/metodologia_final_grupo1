[pytest]
testpaths = src/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --verbose
    --doctest-modules
    --cov=src
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=80